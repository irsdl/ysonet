"""Reference archive library.

Dev-only tooling. Nothing here is shipped with ysonet.exe, and nothing here
imports from `.claude/skills/`: the archive owns its own implementation so a
change made for link curation cannot break archiving, and vice versa. See
`tools/references/README.md` for the responsibility boundary.
"""
