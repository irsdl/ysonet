# Memory index

Git-tracked knowledge for contributors and their agents. See the "Memory Management"
section in `CLAUDE.md` for the rules. Entry format inside each topic file: `date - what - why`.

| File | Description | Last updated |
|---|---|---|
| [interactive-ui.md](interactive-ui.md) | Conventions and gotchas for the interactive menus/screens under `ysonet/Interactive/`, including the four separate discovery surfaces one session-wide visibility flag has to reach, why the module info panel prints its short facts before the unbounded formatter list, and the one-rendered-line-is-one-console-row invariant the in-place redraw rests on (a newline in an option's help stacked the whole columns view, and a frame needs one row MORE than it has lines), and why the option-choice heuristic must not split a comma-containing VALUE into a menu of broken fragments. | 2026-07-28 |
| [testing.md](testing.md) | Test-harness gotchas: the focused-first/final-FULL gadget/plugin workflow, the test-artifact helpers and where tests write files, antivirus/compile hangs, why a failing fire row hides its real error, the payload families that must be fired in a subprocess, when a fire row needs no marker polling at all, how to prove a finalizer effect with a weak reference, how the OOB tier proves an SMB callback through DNS, and how a TFM-stamped child exe reproduces a pre-4.5.2 framework behaviour with no targeting pack and no registry write, why an outbound-callback gadget wants BOTH the loopback and OOB tiers (and what each one alone fails to prove), the generation-only control that proves a gadget does not resolve its own input at build time, the two payload encodings (LosFormatter base64, SoapFormatter split type names) that break a naive payload search, and the quiet-run machinery: why only a hidden DESKTOP (not a console flag) can hide a payload grandchild, the CloseDesktop thread-lifetime rule, why the WER job replaces any product SetErrorMode call, why there is no `crashed` status state, why a rename-publishing status file must retry, why a handle-leak test must collect first, and the two constraints (digit-first tag, space-free path) the fire sink command has to satisfy, and the environment capability gate: why registration is not proof of DNS, why protocol matching must be exact, why an interactsh SMB record has no `full-id` and can only be correlated by cursor, why an absence control shares its positive row's prerequisite, why every automated UNC touch needs a self-hosted server the operator declares they own, why failure counts are recorded rather than derived, and why an HTTPS probe must never install an accept-all certificate callback, the per-run artifact namespace: why every helper-managed artifact lives in `ysonet_run_<pid>_<random>`, the two artifacts that deliberately stay outside it, and why the stale sweep needs owner liveness as well as age, why the hidden desktop cannot contain a console window on Windows 11 (the default-terminal handoff) and what was measured to prove it, when an accept-and-close listener is not enough and a recording HTTP responder is (plus the thread-per-connection rule that keeps a two-request chain from deadlocking), why `-t --debugmode` is not a formatter audit (ShowErrors prints only a stack trace), how a UNC/SMB callback is proven LOCALLY through an 8.3 short name (and the two things that break it: the whole path must resolve, and 8.3 creation is per volume), the bytes-vs-text trap in the shared legacy-XML child, why the plugin/Xps fire rows pass `--rawcmd` (plus the generation-time equality assertion that keeps the wrapper covered and the plugin-statics leak that would otherwise poison later rows), and the three things the isolated self-test needed before a denial-of-service gadget could be `-t`-ed at all (one shared format map, a forced collection in the child, and three reported outcomes instead of two) plus what the suite proves for a payload no test may deserialize, and why a DoS harness drives the product rather than rehosting it (two dead ends: PowerShell assembly resolution, and a construct probe that dies mid-read). | 2026-07-28 |
| [gadgets.md](gadgets.md) | Gadget conventions and traps: the working-version rule for `WithVersions` when latest fails, which JSON escaper a double-quoted template needs (and how fastJSON silently deletes `\'`), that the YAML minifier corrupts text too so every branch needs verifying, which WinForms getter carrier each serializer can build, `CommandInputType.UncPath`, why a `MarshalByRefObject` target loses FsPickler while DataContractJsonSerializer gains an ISerializable constructor with string members, the short-name expansion rule mscorlib really applies, the payload self-containment rule (no gadget payload in a helper or a shared builder) and what it replaced, the write-it-to-be-read rule (research material, payload visible in source, no obfuscation - not to be confused with the Release binary's antivirus string encryption), when MessagePack Typeless actually writes a type name, inner-gadget option leaks, the `Hosted` label and `Generators/HostedPayloads/`, the formatter `(N)` annotation, Soap vs generic roots, TreeSet/SortedDictionary shapes, the sorted-container argument order and its benign-comparison parameter, void sinks in the splice, the local-vs-target command input types, the DataContract shape a field-serialized target needs instead of an ISerializable marshal, the two separate things that rewrite operator text in an XML payload, which serializers can reach a property SETTER on a carrier that implements IList (and the two that fail silently), the SharpSerializer binary type-name swap, the opposite rule for a sink that lives in the ISerializable CONSTRUCTOR, the three document shapes an ISerializable target with one `byte[]` member needs (and why NDCS needs none), the two COM OBJREF facts that decide a DCOM callback gadget's input type, and how to name a "we build the bytes" vs "bring your own bytes" variant pair so nobody reads the second as the RCE one, the printed-vs-resolved split that keeps a not-listed module buildable (plus why an unreadable visibility declaration fails open), why a type that is both `ISerializable` and `IXmlSerializable` silently loses the whole DataContract family, why the two XXE carriers stayed separate gadgets with opposite formatter lists, what a gadget has to prove before it may declare information disclosure, and why a gadget validates the FORM of a target-side identifier as little as possible (with the write-order guarantee that replaces the refusal), the catalogue-wide rule that operator input is documented rather than policed, the three `-t` outcomes (accept, isolate in a child, refuse) plus why the SMB harness gate is not one of them, where the single denial-of-service contract lives and what its one facet turns on (including the toggle rule: the acknowledgement is required on every surface and worded per surface, and interactive use never implies it), why a payload that is only a type name gets the widest formatter list in the catalogue (and the two structural reasons four formats still cannot build it), and when the runtime version axis is correctly left `unspecified` because the gate is an assembly rather than a framework. | 2026-07-28 |
| [cli.md](cli.md) | One-shot CLI conventions in `ysonet/Program.cs`: where `--raf` validates itself and why there is no synthetic gadget name, which modes take precedence over it, the sweep's four-count result contract and exit rule, why sweep cells go through the shared generation core and why `ProcessOutput` returns a reason, and the shared bounded stdin reader. | 2026-07-28 |
| [dependencies.md](dependencies.md) | Why each old/vulnerable library is pinned, which pins are load-bearing gadgets vs bump candidates, the MessagePack 2.5.301 bump and why it was safe, how to restore a `packages.config` project, the `1.3.3.7` assembly-version marker in `ysonet/dlls/`, and where the public triage record lives. | 2026-07-26 |
| [workflow.md](workflow.md) | Git/multi-agent workflow: use a git worktree (or at least a new branch) for new or parallel work so agents don't share one checkout, how to work a `dev-kitchen/todo/` item (fix what you find instead of filing new todo files, ask until nothing is vague, ask before deleting the item), and how to keep building when the maintainer has `ysonet.exe` open (build a second copy to a scratch OutputPath; a copy-step failure still leaves the suite running against a STALE binary). | 2026-07-27 |
| [tooling.md](tooling.md) | Repo hygiene: stray decompiler `.resources` at the repo root, the `/*.resources` gitignore guard, and running decompilers from a scratch dir. | 2026-07-23 |

A contributor may keep unpublished notes in an untracked `private/` folder next to
this index. When `private/index.md` exists, read it as well; it is personal, so never
add its rows here.
